package com.link.newsapp.network

object NetworkConstant {
    const val REQUEST_TIMEOUT = 20L
}

object ArticlesUrl {
    const val articles = "articles"
}

object ArticlesCategories {
    const val associated_press = "associated-press"
    const val next_web = "the-next-web"
}